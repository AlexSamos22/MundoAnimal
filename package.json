{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "homepage": "https://AlexSamos22.github.io/MundoAnimal",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "parcel index.html --dist-dir dist",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "npm run dev && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0"
  }
}
